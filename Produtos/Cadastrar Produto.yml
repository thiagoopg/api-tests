info:
  name: Cadastrar Produto
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/produtos"
  body:
    type: json
    data: |-
      {
        "nome": "Logitech MX Vertical",
        "preco": 470,
        "descricao": "Mouse",
        "quantidade": 381
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
