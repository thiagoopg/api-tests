info:
  name: delete id
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{url}}/usuarios/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
