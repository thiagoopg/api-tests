info:
  name: 08 Excluir carrinho e retornar produtos para estoque
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{url}}/carrinhos/cancelar-compra"
  headers:
    - name: Authorization
      value: "{{token}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Deve retornar status 200 - Ok", function() {
          expect(res.getStatus()).to.equal(200);
        });

        test("Deve confirmar mensagem de sucesso", function() {
          const data = res.getBody();
          expect(data.message).to.equal("Registro excluído com sucesso. Estoque dos produtos reabastecido");
          expect(data.message).to.be.a('string');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
