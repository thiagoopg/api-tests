info:
  name: Listar carrinhos
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/carrinhos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
